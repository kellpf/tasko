import { Routes } from '@angular/router';
import { Board } from './board/board';

export const routes: Routes = [
    // TODO: ADD -> path: ''
    {
        path: 'board',
        component: Board
    }
];
